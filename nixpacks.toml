[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20", "playwright"]

[phases.install]
cmds = [
    "npm install",
    "python3.11 -m pip install -r requirements.txt",
    "npx playwright install chromium --with-deps"
]

[start]
cmd = "node proxy.js"
