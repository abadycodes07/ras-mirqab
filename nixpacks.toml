[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs", "git"]

[phases.install]
cmds = ["npm install", "python3 -m pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "node proxy.js"
