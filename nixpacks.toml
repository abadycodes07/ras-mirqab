[phases.setup]
nixPkgs = ["python3Full", "nodejs", "playwright"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install twscrape apify-client beautifulsoup4",
    "playwright install chromium --with-deps"
]

[start]
cmd = "node proxy.js"
