[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs", "playwright"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install -r requirements.txt",
    "npx playwright install chromium --with-deps"
]

[start]
cmd = "node proxy.js"
